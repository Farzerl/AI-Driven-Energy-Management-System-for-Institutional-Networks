# Data Schema

This folder defines the columns used by the sample meter dataset and the MVP model workflow.

Required columns: timestamp, facility_id, facility_name, sector, kwh, kva, kvarh, power_factor.

Optional columns: voltage_v, current_a, academic_status, occupancy_proxy, weather_temp_c, quality_flag.
