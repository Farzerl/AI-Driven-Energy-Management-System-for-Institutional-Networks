# Software Data Folder

Use this folder for generated and cleared project datasets.

Generate synthetic data with:

```bash
python -m software.ai_engine.dataset_generator
```

Generated CSV files are ignored by Git to avoid committing large files by accident.
