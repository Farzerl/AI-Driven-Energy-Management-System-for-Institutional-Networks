# Repository Alignment Record

Date: 14 July 2026

## Purpose

This record documents the sanitized repository package prepared for AI4I Track 3 technical review.

## Included

- FastAPI service and responsive dashboard;
- aggregate real-data evidence;
- AI-versus-rule comparison;
- simulated edge gateway and meter-ingestion API;
- public-source cost model;
- safe sample data;
- tests, CI, security scan, repository audit;
- architecture, API, user, deployment, business, and risk documentation;
- screenshots and final demo instructions.

## Excluded

- raw UZ workbooks and source ZIP;
- exact private interval records;
- real facility and meter mappings;
- private trained model binaries;
- credentials and completed environment files;
- local `.venv`;
- runtime logs;
- caches;
- duplicate launchers and obsolete workflows;
- empty or placeholder submission folders.

## Authoritative launch

```text
START_AI4I_EMS.bat
```

Edge demonstration:

```text
START_EDGE_DEMO.bat
```

Cost-model rebuild:

```text
RUN_STAGE4_COST_MODEL.bat
```

## Evidence position

The MVP is advisory and operator-confirmed. Edge ingestion is simulated. Cost figures are public-source planning estimates. No live campus commissioning, autonomous switching, invoice value, or realised saving is claimed.

## Validation policy

Historical screenshots and old test counts are not authoritative. Current evidence is generated by:

```powershell
python -m pytest -q
python -m scripts.security_scan --output-dir evidence\validation\security
python -m scripts.repository_audit --output-dir evidence\validation\repository
```

The package manifest and SHA-256 file list are generated after final validation.
